import styled from 'styled-components'

export const LayoutContainer = styled.div`
  max-width: 72rem;
  height: calc(100vh);
  margin: auto;
  padding: 0 1rem;

  display: flex;
  flex-direction: column;
`
